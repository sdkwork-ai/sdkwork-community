[package]
name = "sdkwork-community-commerce-membership-embedded"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
sdkwork-community-service.workspace = true
sdkwork-database-sqlx.workspace = true
# Membership is the dependency application; this consumer-side L4 adapter
# composes its public repository surface in-process (embedded same-process
# deployment) instead of the generated SDK HTTP client.
sdkwork-membership-repository-sqlx = { path = "../../../sdkwork-membership/crates/sdkwork-membership-repository-sqlx" }
